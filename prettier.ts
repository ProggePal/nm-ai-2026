import type { Config } from "prettier";

const config: Config = {
  useTabs: true,
  printWidth: 110,
  singleQuote: true,
  trailingComma: "es5",
  bracketSpacing: true,
  bracketSameLine: false,
  semi: false,
};

export default config;
